kind: Deployment
apiVersion: apps/v1
metadata:
  name: my-emoji-search
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-emoji-search
  template:
    metadata:
      labels:
        app: my-emoji-search
    spec:
      containers:
        - name: my-emoji-search
          image: localhost:5000/my-emoji-search
          imagePullPolicy: Always
          ports:
            - containerPort: 80
      restartPolicy: Always
---
kind: Service
apiVersion: v1
metadata:
  name: my-emoji-search
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 31000
  selector:
    app: my-emoji-search
